# Core dependencies
gradio>=4.0.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
requests>=2.31.0

# ML/AI dependencies
torch>=2.0.0
transformers>=4.57.0
accelerate>=0.20.0
qwen-vl-utils>=0.0.14

# Vector database
faiss-cpu>=1.7.4
# For GPU: use faiss-gpu instead of faiss-cpu

# Video/Audio processing (optional but recommended)
moviepy>=1.0.3
librosa>=0.10.0
soundfile>=0.12.1
SpeechRecognition>=3.10.0

# Optional: for better performance
# flash-attn  # Uncomment if you have CUDA and can compile from source
